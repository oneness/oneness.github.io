#!/usr/bin/env bash
# -e: stop on first failure -u: exit on first unset var -o: first pipefail
set -euo pipefail

# just one liner to get all my stuff into a box
$ sh <(curl -L https://raw.githubusercontent.com/oneness/oneness/refs/heads/main/bootstrap.sh)
